describe('Invoice refunds', () => {
  it('should reject a refund on an invoice that was never issued', () => {});
  it('should reject a second refund on an already refunded invoice', () => {});
  it('must record who authorised the refund', () => {});
});
