# View 3 — Relationship dependency graph

> **Generated by `tools/generate-metamodel-views.py`. Do not edit.**
> A derived artifact (`ADR-0012`). Edit the sources and regenerate.

Every `owl:ObjectProperty` with a declared domain and a named range. Properties whose range was deliberately omitted do not appear — that omission is itself a finding (`FINDINGS.md` #8).

**19 classes connected by 26 properties.**

```mermaid
graph LR
  ContextualEntity[ContextualEntity]
  BoundedContext[BoundedContext]
  ContextualEntity -->|scopedTo| BoundedContext
  BoundedContext -->|contains| ContextualEntity
  BoundedContext -->|relatesTo| BoundedContext
  Invariant[Invariant]
  Artifact[Artifact]
  Invariant -->|enforcedAt| Artifact
  Capability[Capability]
  Capability -->|realisedBy| Artifact
  Actor[Actor]
  Actor -->|uses| Capability
  Evidence[Evidence]
  ArtifactRevision[ArtifactRevision]
  Evidence -->|observedAt| ArtifactRevision
  EvidenceKind[EvidenceKind]
  Evidence -->|ofKind| EvidenceKind
  ArtifactRevision -->|revisionOf| Artifact
  Artifact -->|hasActiveRevision| ArtifactRevision
  DimensionAssignment[DimensionAssignment]
  DimensionAssignment -->|classifies| ArtifactRevision
  Dimension[Dimension]
  DimensionAssignment -->|along| Dimension
  Skill[Skill]
  Skill -->|produces| Artifact
  Workflow[Workflow]
  Workflow -->|produces| Artifact
  WorkflowStep[WorkflowStep]
  Workflow -->|hasStep| WorkflowStep
  WorkflowStep -->|executes| Skill
  EngineeringGate[EngineeringGate]
  Workflow -->|passesThrough| EngineeringGate
  EngineeringGate -->|decidedBy| Actor
  AcceptanceRecord[AcceptanceRecord]
  AcceptanceRecord -->|decidedBy| Actor
  AcceptanceRecord -->|accepts| ArtifactRevision
  AcceptanceRecord -->|reviewedBy| Actor
  AcceptanceRecord -->|cites| Evidence
  ADR[ADR]
  ADR -->|supersedes| ADR
  ADR -->|corrects| ADR
  Issue[Issue]
  ADR -->|resolves| Issue
  StateMachineSpecification[StateMachineSpecification]
  StateMachineSpecification -->|drivenBy| Workflow
```

## Structural metrics

**19 nodes.** Computed from the graph, not read from the specifications.

### Isolated and near-isolated

No isolated nodes.

**Pendant (degree 1):** `ContextualEntity`, `Dimension`, `EvidenceKind`, `Invariant`, `Issue`, `StateMachineSpecification`

### Hubs (degree ≥ 5)

| Node | Degree |
|---|---|
| `Artifact` | 5 |

### Near-identical neighbourhoods (Jaccard ≥ 0.6)

None above threshold.

### Chains (in-degree 1, out-degree 1)

`Capability`, `ContextualEntity`, `EngineeringGate`, `Skill`, `WorkflowStep`

### Repeated motifs (relation used ≥ 3 times)

None.
